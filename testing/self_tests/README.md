# Testing Layer Self Tests

This directory contains tests for SmartTest framework code under `testing/`,
plus adjacent bridge-oriented tests that should not appear as product test cases.

SmartTest case discovery uses `testing/tests/` by default. Keep framework,
runner, runtime, parameter, UI bridge, Jira, and AI module tests here instead of
placing them in the business case tree.
